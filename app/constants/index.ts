// Define some times in milliseconds
export const ONE_MINUTE_IN_MILLISECONDS = 60 * 1000;
export const ONE_HOUR_IN_MILLISECONDS = 60 * 60 * 1000;
export const ONE_DAY_IN_MILLISECONDS = 24 * 60 * 60 * 1000;

// Define common index table list limit
export const ITEM_LIST_LIMITATION = 250;

// Define ID for contextual save bar
export const CONTEXTUAL_SAVE_BAR_ID = "designify-contextual-save-bar";

// Empty array
export const EMPTY_ARRAY: any[] = [];
